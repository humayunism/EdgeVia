apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fairflow-proxy-hpa
  namespace: fairflow
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fairflow-proxy
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
